# Cursor Rules для проекта Iriska

## 🧠 **Локальная память Cursor**
Папка `.cursor-memory/` содержит локальную память проекта для Cursor. 
ВСЕГДА обращайся к этой папке для получения контекста проекта.

### **Структура памяти:**
- `.cursor-memory/project/` - описание проекта и продукта
- `.cursor-memory/technical/` - техническая документация и архитектура  
- `.cursor-memory/context/` - текущий рабочий контекст и задачи
- `.cursor-memory/archive/` - завершенные задачи

### **Правила работы:**
1. **ПРИ КАЖДОМ запросе** сначала проверяй `.cursor-memory/context/activeContext.md`
2. **Для технических решений** используй `.cursor-memory/technical/`
3. **Для понимания целей** обращайся к `.cursor-memory/project/`
4. **ВСЕГДА обновляй** файлы в `.cursor-memory/` при изменении проекта
5. **Не анализируй весь проект** - используй локальную память, и только затем анализируй недостающую информацию в самом проекте

### **Приоритетные файлы:**
- `.cursor-memory/context/activeContext.md` - текущие задачи
- `.cursor-memory/context/tasks.md` - план реализации
- `.cursor-memory/context/progress.md` - прогресс проекта

## 🚀 **Автоматические действия:**
- При завершении задачи - архивируй в `.cursor-memory/archive/`
- При изменении статуса - обновляй `.cursor-memory/context/progress.md`
- При новых решениях - документируй в соответствующих папках
- При изменении архитектуры - обновляй `.cursor-memory/technical/`

## 📝 **Формат обновлений:**
Все обновления в `.cursor-memory/` должны быть:
- Структурированными и читаемыми
- С эмодзи для лучшей навигации
- С указанием даты и статуса
- Связанными с соответствующими файлами кода
